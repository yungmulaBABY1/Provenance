| Finding | Type | Location A | Location B | Identical / divergent / contradictory | What breaks |
|---|---|---|---|---|---|
| `[P1]` construct-side retrieval gate is copied into both files. | duplication | `1-claim-extraction.txt`, lines 106-134: "CONSTRUCT-SIDE RETRIEVAL GATE... ESTABLISHED-REPORTED... ESTABLISHED-CITED... ASSERTED... UNKNOWN... ITEMS-RETRIEVED... ITEMS-INFERRED..." | `2-construct-validity.txt`, lines 152-180: the same block from "CONSTRUCT-SIDE RETRIEVAL GATE" through "State the retrieval basis for every construct-side finding." | identical - watch-point | No current functional risk. The two operational copies can drift independently. |
| `[P2]` grade-before-assert gate is copied into both files. | duplication | `1-claim-extraction.txt`, lines 136-152: "GRADE-BEFORE-ASSERT... ESTABLISHED-FALSE CONTESTED FRINGE..." | `2-construct-validity.txt`, lines 182-198: the same block through "the construct is contested alone is not a finding." | identical - watch-point | No current functional risk. The two operational copies can drift independently. |
| The declared `[P2]` grade set omits the value required for the accurate-belief branch. | fork | `1-claim-extraction.txt`, lines 138-144: "grade the specific belief: ESTABLISHED-FALSE CONTESTED FRINGE." | `1-claim-extraction.txt`, lines 146-147: "fires at strength only where the penalized version is ESTABLISHED-ACCURATE." The same split appears in `2-construct-validity.txt`, lines 184-193. | contradictory | `ESTABLISHED-ACCURATE` is load-bearing but is not one of the declared grade values, so a pathologization finding cannot be encoded using the stated closed set. |
| `CONTESTED` names both a verification state and a belief grade. | fork | `1-claim-extraction.txt`, line 74: "CONTESTED: credible disagreement exists; who is correct remains unresolved." `2-construct-validity.txt`, line 138, repeats this verification state. | `1-claim-extraction.txt`, lines 141 and 149: `CONTESTED` is one of the belief grades that makes a wrongness/pathologization finding "weak or void." `2-construct-validity.txt`, lines 187 and 195, repeats this use. | divergent | The same token identifies two different axes: source-verification status and substantive belief grade. A finding can be labeled `CONTESTED` without showing which axis is meant. |
| `UNKNOWN` is outside the declared operationalization-status set but is then used as an operationalization status. | fork | `1-claim-extraction.txt`, lines 124-129: "Operationalization status... ITEMS-RETRIEVED... ITEMS-INFERRED." | `1-claim-extraction.txt`, lines 182-183: "If ITEMS-INFERRED, the operationalization is UNKNOWN." The same pattern appears in `2-construct-validity.txt`, lines 170-175 and 357-359. | contradictory | A run can emit either `ITEMS-INFERRED` or `UNKNOWN` for the same operationalization, although only the two `ITEMS-*` values are declared as operationalization statuses. |
| The items-retrieval gate is restated locally with different completeness. | duplication | `1-claim-extraction.txt`, lines 172-193: the definition-operationalization check bars inference from a scale name or related scale because it "manufactures false scope creep." | `2-construct-validity.txt`, lines 350-362: "Run the same check as the claim map under the compact items gate," but the local restatement omits the explicit inference bar and its rationale. | divergent | A run following only the local construct section receives a weaker version of the gate than a run following the claim-map wording or compact primitive. |
| `coextensive / narrower / broader` is repeated in both operational checks. | duplication | `1-claim-extraction.txt`, lines 182-193: do not classify when items are inferred; when retrieved, classify "coextensive; narrower; or broader." | `2-construct-validity.txt`, lines 352-362: the same three values and the same items-retrieval condition. | identical vocabulary - watch-point | No current functional risk. The classification vocabulary is duplicated and can drift independently. |
| `Definitional scope creep` is classified once as a severity and elsewhere as a failure mode. | fork | `2-construct-validity.txt`, lines 382-389, under "Severity options": "Definitional scope creep (operationalization broader than stated definition)." | `2-construct-validity.txt`, lines 780-816, under "FAILURE-MODE CLASSIFICATION": `[ ] Definitional scope creep...` | contradictory | The same term can be entered in either the `Severity` field or the `Failure mode` field, producing structurally incompatible rows. |
| The Section 3 severity vocabulary does not match the standardized finding-table severity enum. | fork | `2-construct-validity.txt`, lines 382-389: "No meaningful gap / Minor limitation / Moderate proxy gap / Major construct mismatch / Fatal proxy failure... / Definitional scope creep." | `2-construct-validity.txt`, line 904: "Severity | none / minor / moderate / major / fatal-for-strong-claim." | divergent | A Section 3 result cannot be copied literally into the final `Severity` field; "definitional scope creep" has no final severity value, while the other labels lose their mechanism-specific wording. |
| `[P2]` uses `weak` and `void` as outcomes, but neither is in the severity enum. | fork | `1-claim-extraction.txt`, line 149, and `2-construct-validity.txt`, line 195: "CONTESTED or FRINGE makes the finding weak or void." | `2-construct-validity.txt`, line 904: "none / minor / moderate / major / fatal-for-strong-claim." | divergent | There is no defined conversion from `weak` or `void` into the standardized severity values, so models can encode the same grade differently. |
| Construct-establishment property names are not preserved across the map and Section 16. | fork | `1-claim-extraction.txt`, lines 205-210: `PREVALENCE`, `FORM / INTENSITY`, `DOMAIN`, `WRONGNESS MAP`, `HARM-IN-EFFECT`. | `2-construct-validity.txt`, lines 814-816: "Construct asserted, not established (prevalence / form / domain / harm)" plus a separate checkbox, "Wrongness asserted wholesale..." | divergent | `FORM / INTENSITY` becomes `form`; `HARM-IN-EFFECT` becomes `harm`; `WRONGNESS MAP` is removed from the construct-establishment property set and split into a different failure mode. The map and prosecutor no longer have a one-to-one property vocabulary. |
| The compact property header already uses shorter names than the claim-map extraction. | fork | `1-claim-extraction.txt`, line 112, and `2-construct-validity.txt`, line 158: "prevalence / form / domain / wrongness / harm." | `1-claim-extraction.txt`, lines 205-210: "PREVALENCE / FORM / INTENSITY / DOMAIN / WRONGNESS MAP / HARM-IN-EFFECT." | divergent | The compact primitive and the executable claim-map check do not expose the same property names, allowing `form` and `harm` to be interpreted more broadly than the mapped `FORM / INTENSITY` and `HARM-IN-EFFECT`. |
| `ESTABLISHED-CITED` gains a required qualifier only in the construct prosecutor. | fork | `1-claim-extraction.txt`, line 116: "ESTABLISHED-CITED: established in cited prior work you actually checked." The claim-map property check, lines 202-215, records that status without a contested/uncontested subtype. | `2-construct-validity.txt`, lines 295-312: every `ESTABLISHED-CITED` property must be classified "uncontested in the respect the paper requires" or "contested in the respect the paper requires," and the subtype controls the not-firing condition. | divergent | The claim-map handoff value `ESTABLISHED-CITED` is insufficient to determine whether construct establishment passes or remains live; the prosecutor must introduce an unrepresented second-stage qualifier. |
| Section 17 cannot represent every construct-establishment finding created by Section 1A. | fork | `2-construct-validity.txt`, lines 295-309: an `ESTABLISHED-CITED` property can still produce a live finding when the citation is overextended. | `2-construct-validity.txt`, lines 845-847: "Construct-establishment gap... list ASSERTED / UNKNOWN properties..." | contradictory | A cited-but-overextended construct-establishment finding is neither `ASSERTED` nor `UNKNOWN`, so it has no stated place in the final summary field. |
| The construct-establishment mechanism has two different output names. | fork | `2-construct-validity.txt`, line 814: "Construct asserted, not established." | `2-construct-validity.txt`, lines 845-847: "Construct-establishment gap." | divergent | The Section 16 checkbox and Section 17 summary cannot be matched by literal failure-mode name, weakening mechanical traceability. |
| The definition-operationalization mechanism has a specific and a generic output name. | fork | `2-construct-validity.txt`, lines 361-362, 389, and 810: "DEFINITIONAL SCOPE CREEP" / "Definitional scope creep." | `2-construct-validity.txt`, line 845: "Definition-operationalization mismatch." | divergent | The final summary can record an unspecified mismatch without preserving whether the measure was coextensive, narrower, or broader, or whether the checked failure mode was specifically scope creep. |
| "Failure mode" refers to two incompatible taxonomies, making the final-table pointer ambiguous. | broken-ref | `2-construct-validity.txt`, lines 603-610: "Measurement role or failure mode" values are `Direct measure / Proxy evidence / Weak proxy / Conceptual claim / Citation-laundered premise / Other construct-validity failure mode`. | `2-construct-validity.txt`, lines 780-818: a much larger failure-mode checklist. The standardized table at line 903 says only: "Failure mode | Construct-validity taxonomy used above." | contradictory | "Taxonomy used above" has at least two live targets. A standardized row can use incompatible labels depending on whether the auditor follows Section 11 or Section 16. |
| The newly defined construct-property and operationalization statuses have no dedicated field in the claim-map output schema. | fork | `1-claim-extraction.txt`, lines 112-129: separate "Construct-property status" and "Operationalization status" vocabularies are mandatory. | `1-claim-extraction.txt`, lines 730-738: the required table contains only `Field / Content / Verification state`. | contradictory | The new status axes must be buried in `Content`, omitted, or incorrectly placed in `Verification state`; the schema does not preserve them as separate axes. |
| The standardized construct finding table also has no field for either `[P1]` status axis. | fork | `2-construct-validity.txt`, lines 158-180: separate construct-property and operationalization-status vocabularies. | `2-construct-validity.txt`, lines 897-913: the final table includes `Failure mode`, `Severity`, `Verification state`, and `Status`, but no construct-property-status or operationalization-status field. | contradictory | A final row cannot carry the retrieval status that controls whether a construct-side finding may be load-bearing, except by embedding it in free text. |
| `CONTESTED` and `PROVISIONAL` are reused in the undefined final `Status` field. | fork | `2-construct-validity.txt`, lines 128-142: `CONTESTED` and `PROVISIONAL` are defined verification states. | `2-construct-validity.txt`, line 913: `Status | confirmed / contested / provisional`, with no separate definition of what `Status` measures. | divergent | The final row has two fields that can contain `contested` or `provisional` - `Verification state` and `Status` - without defining how the axes differ. |
